import style from 'styled-components';

export const ConfigContainer = style.div`
    display: flex;
    flex-wrap: wrap;
`;

export const KpiContainer = style.div`
    marginBottom: 1rem;
    border: 1px solid #ddd;
    padding: 1rem;
`;

export const ButtonsContainer = style.div`
    display: flex;
    justify-content: center;
`;

export const ButtonContainer = style.div`
    margin-top: 1rem;
    margin-bottom: 1rem;
    margin-right: 3rem;
`;
