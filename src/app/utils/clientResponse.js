
// pour créer un retour client rapide // genre comme une alerte (pas en core utlisé)
export function injectClientResponse (message){

    let body = document.querySelector("body");
    console.log(body);

    body.insertAdjacentElement("beforeend" , )
    return message;
}